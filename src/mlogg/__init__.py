"""Initialization logic for mlogg logging configuration."""

from src.mlogg.config import init_logging


def init_custom_logging():
    """Initialize logging for the app (compact version)."""
    init_logging()
